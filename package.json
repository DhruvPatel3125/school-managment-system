{
  "name": "educore-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EduCore Multi-Tenant School Management System Monorepo",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build:client": "npm run build --workspace=client",
    "build": "npm run build:client"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}

